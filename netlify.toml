[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.11.0"

# SPA routing redirect for client-side navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
